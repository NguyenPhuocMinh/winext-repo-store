'use strict';

const transformText = (string) => {
  let text = '';
  if (string === 'Required') {
    text = 'Bắt buộc nhập';
  }
  if (string === 'IncorrectFormat') {
    text = 'Chưa đúng định dạng';
  }
  return text;
}

module.exports = transformText;